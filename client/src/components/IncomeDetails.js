import React from "react";

function IncomeDetails({
    id,
    title,
    amount,
    date,
    category, 
    description
}) {
  console.log(amount)
  return (
    <div>

    </div>
  )}


export default IncomeDetails;
