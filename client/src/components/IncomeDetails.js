import React from "react";

function IncomeDetails({
    id,
    title,
    amount,
    date,
    category, 
    description
}) {
console.log(title)
console.log(amount)
  return (
    <div>
<h1>{title}</h1>
<h1>{title}</h1>
<h1>{title}</h1>
    </div>
  )}


export default IncomeDetails;
